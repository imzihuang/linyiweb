1、git版本管理
	a、每个人都自己建立一个分支。建立新分支之前，更新最新代码。
	b、以任务为准，当前任务结束，合并到主分支，并删除当前分支。
2、项目进度
	a、已完成：
		网站框架
		店铺列表插件（demo）
		数据设计、测试数据写入
	b、待进行：
		注册界面（邮箱验证）
		登录界面
		主界面
		店铺展示界面
		商品筛选样式，插件
		安装配置redis数据库，编写demo
3、任务安排
	学龙：
	注册界面、登录界面、主界面的设计。
	把插件熟悉下，争取下周能自己写些简单的插件。如不绑定内部事件、不需要绑定内部ajax请求等。

	文清：
	根据主界面，组织下数据，分装逻辑方法。
	根据redis的demo，实现之前说的微信功能。
	设计下商品筛选的逻辑。（考虑下当前数据结构是否支持）

	子皇：
	实现注册和登录功能（我这里有现成代码，稍微调整）。
	店铺展示主页实现。



